const initState = {

}
type actionType = {
    type: string,
    payload: any
}
export function reducer(state: any = initState, action: actionType) {
    switch (action.type) {

        default:
            break;
    }
}